overwrite: true
schema: './src/schema.graphql'
generates:
  ./src/schemaTypes.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      federation: true
      useIndexSignature: true
